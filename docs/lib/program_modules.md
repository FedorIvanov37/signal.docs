## Program Modules

Program modules contain the actual application logic.

These are the working components such as `Parser`, `Terminal`, `Connector`, `Queue`, `FieldsGenerator`, `Logger`, and many others.

Each module performs a specific task: parsing incoming dumps, generating fields, managing TCP connections, sending transactions, matching responses, writing logs, validating data, and coordinating the transaction lifecycle.
